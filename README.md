general feature extraction for audio signals:
First, you should load your own audio signal. 
You don't need to make any changes. 
You can run each cell to detect any abnormalities in your signal.

cut error detection:
fist you should put your audio signal on a folder named 'extract', the model process reads the file from extract folder and processes them. If there is an error, the audio file can move to another folder, named 'error' else it will be deleted. 
